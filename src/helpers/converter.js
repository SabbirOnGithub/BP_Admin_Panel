const fileToBase64 = (file) =>{
    // console.log(file)
    let reader = new FileReader();
    console.log(file)
    // file  && console.log(reader.readAsDataURL(file))
    // return file ? reader.readAsDataURL(file) : '#';
}
export { fileToBase64 };